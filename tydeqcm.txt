<!DOCTYPE html>
<html lang="mg">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Miandry kely - Fanandramana</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: #0f172a;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: system-ui, -apple-system, 'Segoe UI', sans-serif;
            padding: 20px;
        }

        /* Carte simple */
        .card {
            background: #1e293b;
            border-radius: 28px;
            padding: 40px 32px;
            text-align: center;
            max-width: 450px;
            width: 100%;
            box-shadow: 0 20px 35px -10px rgba(0,0,0,0.4);
            border: 1px solid #334155;
        }

        /* Animation très simple : cercle qui tourne */
        .loader {
            width: 60px;
            height: 60px;
            margin: 0 auto 24px auto;
            border: 4px solid #475569;
            border-top: 4px solid #3b82f6;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Texte malgache */
        h1 {
            font-size: 1.8rem;
            color: #f1f5f9;
            margin-bottom: 12px;
            font-weight: 600;
        }

        .sub {
            color: #94a3b8;
            font-size: 1rem;
            margin-bottom: 20px;
        }

        /* trois petits points très simples */
        .dots {
            display: flex;
            justify-content: center;
            gap: 8px;
            margin: 20px 0 10px 0;
        }

        .dot {
            width: 8px;
            height: 8px;
            background: #60a5fa;
            border-radius: 50%;
            animation: bounce 1.2s infinite ease;
        }

        .dot:nth-child(2) { animation-delay: 0.2s; background: #a78bfa; }
        .dot:nth-child(3) { animation-delay: 0.4s; background: #f472b6; }

        @keyframes bounce {
            0%, 80%, 100% { transform: scale(0.6); opacity: 0.4; }
            40% { transform: scale(1.1); opacity: 1; }
        }
    </style>
</head>
<body>
    <div class="card">
        <!-- Animation simple : spinner -->
        <div class="loader"></div>

        <!-- Petit effet de points -->
        <div class="dots">
            <span class="dot"></span>
            <span class="dot"></span>
            <span class="dot"></span>
        </div>

        <!-- Message en malgache -->
        <h1>⚡ Miandry kely ⚡</h1>
        <div class="sub">
            Mbola tsy vita ny exercice
        </div>
    </div>
</body>
</html>